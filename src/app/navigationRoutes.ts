import { appRoutes } from 'app';

export const navigationRoutes = [
  {
    name: appRoutes.home.name,
    slug: appRoutes.home.slug,
  },
  {
    name: appRoutes.films.name,
    slug: appRoutes.films.slug,
  },
  {
    name: appRoutes.people.name,
    slug: appRoutes.people.slug,
  },
  {
    name: appRoutes.planets.name,
    slug: appRoutes.planets.slug,
  },
  {
    name: appRoutes.species.name,
    slug: appRoutes.species.slug,
  },
  {
    name: appRoutes.starships.name,
    slug: appRoutes.starships.slug,
  },
  {
    name: appRoutes.vehicles.name,
    slug: appRoutes.vehicles.slug,
  },
];
